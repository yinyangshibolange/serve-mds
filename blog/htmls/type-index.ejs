<!DOCTYPE html>
<html>
 <head></head>
 <body>
<section class="module">
 <ul id="mainList">
  <% if(type_mds) { %>
  <%   type_mds.forEach(function(item){ %>
   <%- `<li>${item.title}</li>`  %>
  <% }) %>
  <% } %>
 </ul>
</section>
 </body>
</html>
